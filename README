version 2 of onspot data management system with agent feature

git clone repo
cd onspot_v2
python manage.py syncdb
python manage.py runserver

Needs xlrd and xlwt modules to make use of 'upload excel data' feature
